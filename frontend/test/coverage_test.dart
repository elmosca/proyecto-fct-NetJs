import 'package:flutter_test/flutter_test.dart';

// Este archivo se usa para configurar coverage de tests
// Los tests reales están en los archivos específicos de cada componente

void main() {
  group('Coverage Configuration', () {
    test('should have basic test setup', () {
      expect(true, isTrue);
    });
  });
}
